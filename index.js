const screenshot = require('./src/screenshot')
const keypress = require('./src/keyPress')
const mouse = require('./src/mouse')
const window = require('./src/findWindow')

module.exports = {
    screenshot,
    keypress,
    mouse,
    window,
}

